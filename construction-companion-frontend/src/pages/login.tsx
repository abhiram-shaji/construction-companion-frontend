/* login.tsx

### **1. Login Page**

**Purpose**: Authenticate users (Admins and Workers) before accessing the app.

**Functionalities**:

- Input credentials (email and password) for login.
- Validate user roles (Admin or Worker) and redirect to appropriate dashboards.
- Display error messages for incorrect credentials.

**UI Components**:

- **Form**:
    - Email input field.
    - Password input field.
    - "Login" button.
- **Error Message**:
    - Text component for invalid login attempts. */